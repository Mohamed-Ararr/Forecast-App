import "package:flutter/material.dart";

import "Widgets/CityViewBody.dart";

class CityView extends StatelessWidget {
  const CityView({super.key});

  @override
  Widget build(BuildContext context) {
    return const CityViewBody();
  }
}
